# Database Module

Future home for MongoDB Atlas connection helpers, repositories, and database setup scripts.

Keep credentials in environment variables and never commit real MongoDB connection strings.
